# デジタルツール管理ダッシュボード (Cloud版)

児童が学習で使用するデジタルツールを教科ごとに管理できるWebアプリケーションです。データの保存に **Firebase Cloud Firestore** を使用しており、複数の端末でデータを共有できます。

## 使い方

### アプリを起動する
`index.html` をブラウザで開いてください（ダブルクリック）。

### ツールを追加・編集・削除する
- **追加**: 右上の「＋ ツールを追加」ボタンから登録
- **編集**: 各カードの「編集」ボタンから変更
- **削除**: 各カードの🗑ボタンから削除

### 教科でフィルターする
- 上部の教科タブをクリックして表示を絞り込めます。

## データの保存について
- データは Google Firebase のクラウド上に保存されます。
- インターネットに接続されている必要があります。
- **注意**: Firestore のセキュリティルールを「テストモード」にしている場合、有効期限（通常30日）が切れるとデータにアクセスできなくなります。継続的に使用する場合は、適切なセキュリティルールの設定を推奨します。

### Firebaseのセキュリティルール設定例 (簡易版)
Firebaseコンソールの Firestore > ルール タブで以下のように設定すると、誰でも読み書きできるようになります（開発・少人数利用向け）。

```js
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```
